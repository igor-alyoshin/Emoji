<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/emojiKeyboardView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="androidx.constraintlayout.widget.ConstraintLayout">

    <io.wola.app.EmojiTabLayout
        android:id="@+id/emojiViewTab"
        android:layout_width="match_parent"
        android:layout_height="@dimen/emoji_header_height"
        app:tabPaddingStart="@dimen/emoji_tab_padding"
        app:tabPaddingEnd="@dimen/emoji_tab_padding"
        android:background="@color/emoji_tab_background_color"
        android:orientation="horizontal" />

    <View
        android:id="@+id/emojiViewDivider"
        android:layout_width="match_parent"
        android:background="@color/emoji_divider_color"
        app:layout_constraintTop_toBottomOf="@id/emojiViewTab"
        android:layout_height="@dimen/emoji_divider_height" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/emojiViewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/emojiViewDivider"
        app:layout_constraintBottom_toBottomOf="parent"/>
</merge>